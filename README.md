- app.get(canciones) => lee json con canciones
- app.post(canciiones) => publica canciones a json
- app.put('/canciones/:id') => modifica canciones del json
- app.delete('/canciones/:id') => elimina canciones de json
